import React from 'react';



const Betweenuname = () => {

const [username, setUsername] = useState();
const [email, setEmail] = useState();
const [textnya, setTextnya] = useState("Please kindly leave your email address below in case  our communication break or 
our customer service not available at this moment");

let emailbaru;


function handleSubmit(event: React.MouseEvent<HTMLInputElement>){
if(event.target.name
}
}

function handleChange(event: React.ChangeEvent<HTMLInputElement>){
       if(event.target.name === "myinput"){
            
              emailbaru = event.target.value;
}
}

return(
<>
<form onSubmit="handleSubmit()">
<p>{textnya}</p>
<input type="text" name="myinput" size={20} onChanged={() => handleChange()} /><input type="submit" value=" > " />
</form>
</>
)


function handleSubmit() {
  


}


}

export default Betweenuname;
